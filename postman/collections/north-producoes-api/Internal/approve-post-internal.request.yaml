$kind: http-request
name: 'Approve Post (Internal/n8n)'
method: PATCH
url: '{{baseUrl}}/api/internal/approvals/post/{{postId}}/approve'
order: 1000
auth:
  type: noauth
headers:
  - key: X-Internal-Api-Key
    value: '{{internalApiKey}}'
pathVariables:
  - key: postId
    value: '{{postId}}'

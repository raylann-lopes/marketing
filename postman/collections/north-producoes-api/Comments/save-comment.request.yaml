$kind: http-request
name: Save Comment
method: POST
url: '{{baseUrl}}/api/comments/save'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
body:
  type: json
  content: |-
    {
      "postId": 1,
      "text": "Comentário de exemplo"
    }

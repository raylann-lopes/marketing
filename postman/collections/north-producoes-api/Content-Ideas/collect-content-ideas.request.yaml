$kind: http-request
name: 'Collect Content Ideas (Admin)'
method: POST
url: '{{baseUrl}}/api/content-ideas/collect/{{clientId}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: clientId
    value: '1'

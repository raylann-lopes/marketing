$kind: http-request
name: Reject Post
method: PATCH
url: '{{baseUrl}}/api/post-approvals/reject/{{postId}}'
order: 5000
pathVariables:
  - key: postId
    value: '{{postId}}'
body:
  type: json
  content: |-
    {
      "rejectionReason": "Motivo da rejeição"
    }

$kind: http-request
name: Search Instagram Signals
method: POST
url: '{{baseUrl}}/api/apify/content-ideas/{{id}}/signals'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: id
    value: '1'

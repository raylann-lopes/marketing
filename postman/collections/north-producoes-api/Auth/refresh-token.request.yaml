$kind: http-request
name: Refresh Token
method: POST
url: '{{baseUrl}}/api/auth/refresh'
order: 3000
auth:
  type: noauth
body:
  type: json
  content: |-
    {
      "refreshToken": "{{refreshToken}}"
    }
